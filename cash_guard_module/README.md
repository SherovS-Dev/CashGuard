# 🔐 Безопасность SecureVault

## Обзор системы безопасности

SecureVault использует многоуровневую систему защиты данных пользователя.

## Уровни защиты

### 1. 🔑 Защищенное хранилище (Secure Storage)

#### iOS - Keychain
- **Что это**: Встроенная система Apple для безопасного хранения паролей и ключей
- **Особенности**:
    - Аппаратное шифрование через Secure Enclave
    - Данные защищены паролем устройства
    - Автоматическое удаление при взломе устройства
    - Изоляция между приложениями

**Настройка в приложении:**
```dart
KeychainAccessibility.first_unlock
```
Это означает, что данные доступны после первой разблокировки устройства.

#### Android - KeyStore
- **Что это**: Системное хранилище криптографических ключей Android
- **Особенности**:
    - Аппаратная поддержка шифрования (на поддерживаемых устройствах)
    - Ключи не могут быть извлечены из KeyStore
    - Защита от root-доступа на большинстве устройств
    - EncryptedSharedPreferences для дополнительного шифрования

**Настройка в приложении:**
```dart
AndroidOptions(encryptedSharedPreferences: true)
```

### 2. 🔐 Криптографическое хеширование

**Алгоритм**: SHA-256

**Процесс**:
```
Пароль пользователя → SHA-256 → Хеш (64 символа)
```

**Пример**:
```
Ввод: "мой_пароль"
Хеш: "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"
```

**Почему это безопасно**:
- Невозможно восстановить исходный пароль из хеша
- Даже при утечке хеша, злоумышленник не узнает пароль
- Одинаковые пароли всегда дают одинаковый хеш (для проверки)

### 3. 👁️ Биометрическая аутентификация

**Поддерживаемые методы**:
- Face ID (iOS)
- Touch ID (iOS)
- Отпечаток пальца (Android)
- Распознавание лица (Android)

**Безопасность**:
- Биометрические данные НЕ хранятся в приложении
- Используется только нативный API операционной системы
- Биометрия работает на аппаратном уровне
- Приложение получает только результат: успех/неудача

## Схема работы безопасности

### Первая установка пароля:
```
1. Пользователь вводит пароль: "мойпароль123"
2. Пароль хешируется SHA-256: "a1b2c3d4e5f6..."
3. Хеш сохраняется в Secure Storage (Keychain/KeyStore)
4. Исходный пароль НИКОГДА не сохраняется
```

### Вход с паролем:
```
1. Пользователь вводит пароль: "мойпароль123"
2. Пароль хешируется SHA-256: "a1b2c3d4e5f6..."
3. Хеш сравнивается с сохраненным хешем
4. Если совпадает → Доступ разрешен
```

### Вход с биометрией:
```
1. Пользователь прикладывает палец/смотрит в камеру
2. ОС проверяет биометрию на аппаратном уровне
3. ОС возвращает результат приложению (без передачи самих данных)
4. Если успех → Доступ разрешен
```

## Сравнение с обычным хранением

| Метод | Безопасность | Доступ при root/jailbreak |
|-------|--------------|---------------------------|
| Обычный файл | ❌ Очень низкая | ✅ Легко читается |
| SharedPreferences | ❌ Низкая | ✅ Легко читается |
| **Secure Storage** | ✅ **Высокая** | ❌ **Защищено** |

## Что НЕ может защитить приложение

1. **Скриншоты**: Пользователь может сделать скриншот после входа
2. **Физический доступ**: Если злоумышленник знает пароль устройства и код приложения
3. **Разблокированное устройство**: Если устройство уже разблокировано

## Рекомендации пользователям

✅ **Используйте сильный пароль**:
- Минимум 8 символов
- Комбинация букв, цифр и символов
- Не используйте очевидные пароли

✅ **Включите биометрию**:
- Быстрее и безопаснее обычного пароля
- Защищает от подсматривания при вводе

✅ **Защитите само устройство**:
- Установите надежный PIN/пароль на устройство
- Включите автоблокировку

❌ **Избегайте**:
- Не сообщайте пароль другим
- Не записывайте пароль в незащищенных местах
- Не используйте один и тот же пароль везде

## Технические детали

### Где хранятся данные?

**iOS**:
```
/var/Keychains/keychain-2.db (системный файл)
Доступ: только через Keychain API
```

**Android**:
```
/data/data/[package_name]/shared_prefs/FlutterSecureStorage
Шифрование: AES-256 через AndroidKeyStore
```

### Использованные библиотеки

```yaml
flutter_secure_storage: ^9.0.0
  - iOS: Использует Keychain Services API
  - Android: Использует AndroidKeyStore + EncryptedSharedPreferences

crypto: ^3.0.3
  - SHA-256 хеширование
  - Чистая реализация Dart (без нативного кода)

local_auth: ^2.2.0
  - iOS: LocalAuthentication framework
  - Android: BiometricPrompt API
```

## Аудит безопасности

### Проверенные сценарии:
- ✅ Защита от простого извлечения файлов
- ✅ Защита от дампа памяти (в большинстве случаев)
- ✅ Защита от перехвата при вводе (нет логирования)
- ✅ Защита от брутфорса (на уровне ОС)

### Потенциальные уязвимости:
- ⚠️ Устройства с root/jailbreak (частичная защита)
- ⚠️ Отладчики при подключенном USB debug
- ⚠️ Продвинутые malware с правами суперпользователя

## Соответствие стандартам

- ✅ OWASP Mobile Security Guidelines
- ✅ Apple App Store Security Requirements
- ✅ Google Play Security Best Practices
- ✅ GDPR (данные только локально)

## Обновления безопасности

Регулярно обновляйте зависимости:
```bash
flutter pub upgrade
```

Следите за обновлениями безопасности:
- flutter_secure_storage
- local_auth
- Flutter SDK

---

**Последнее обновление документа**: 2025-11-01